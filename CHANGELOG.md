# 更新日志 Changelog

本文件记录「一闲笔记 v3（Yixian Notes）」的版本更新历史。

版本号遵循语义化版本（SemVer）：`主.次.修订`。
- **主版本 major**：不兼容的架构/重大改动
- **次版本 minor**：新增功能
- **补丁版本 patch**：Bug 修复 / 小优化

作者：梦一闲

---

## [Unreleased]

### 新增
- 待补充

### 修复
- 待补充

## [3.2.0] - 2026-09-08

### 新增
- 富文本编辑器内核重构：由 contentEditable / document.execCommand 迁移为 TipTap（ProseMirror）模型驱动，跨浏览器（Chrome / Firefox / Safari / Edge）行为一致。
- 行内样式增强：行内代码、上标/下标、多色高亮、文字颜色、字号、字体、行高、删除线、清除格式。
- 段落与块级：段落对齐（左/中/右）、多级列表缩进、任务列表、水平分割线、插入当前日期。
- 表格编辑增强：插入后支持增删行/列、合并/拆字形单元、切换表头行、删除表格。
- 目录（TOC）：自动提取 H1–H3 标题生成目录，一键跳转，可在编辑器左侧/右侧自由切换位置。
- 图片直接粘贴 / 拖拽插入（base64 内联）。
- 工具栏按功能分组重构，分组清晰并支持按分组折行。

### 修复
- 移除对已废弃 document.execCommand / queryCommandState 的依赖，改为原生命令与状态 API：
  - 修复链接、图片输入后因失焦导致选区丢失、插入失效。
  - 撤销/重做改用 ProseMirror 原生 history，替换 innerHTML 快照，避免大型富文本卡顿。
  - 修复 Markdown ⇄ HTML 转换丢失 `<pre>` 代码块、`<blockquote>` 引用的问题。
- 统一工具栏状态（高亮、颜色等选中态）并修复相关历史遗留问题。

### 变更
- 性能优化：工具栏状态由事务 / selection 事件驱动（移除高频轮询 handler），富文本渲染更流畅。
- HTML ⇄ Markdown 转换器统一复用内部 noteImport 转换，减少重复逻辑。

### 作者
- 梦一闲

---

## [3.1.4] - 2026-09-07

### 修复
- 修复加密笔记解锁后内容丢失：会话内重加密调用时误把标题字符串当作正文对象传入，导致每次保存后密文被写坏、再次解锁内容变空；现按《标题 + 正文 + 摘要》完整对象重加密写回。
- 修复加密笔记标题无法修改：标题输入框此前在笔记加密后即被永久禁用，现改为仅在「未解锁」时禁用，解锁后允许修改标题并随正文重加密保存。
- 修复加密笔记在 Markdown 模式下解锁后正文显示为空：解锁时同步生成 Markdown 源码，切换任意视图都不丢内容。
- 修复加密笔记版本历史「当前版本」字数仍为 0：改为按解密后的明文统计字数与摘要，而非实时编辑 DOM。

### 新增
- 快捷新建笔记（全局悬浮弹窗）：修复无法保存，支持默认富文本 + 快捷插入工具栏 + 一键切换 Markdown（复用主窗口 Markdown 编辑器），保存后弹窗内即时预览新建内容，主窗口同步出现并自动选中。
- 全局悬浮弹窗（新建 / 快速打开 / 本地文件搜索 / 剪贴板）支持窗口拖动，并优化交互与 UI。
- 扩充 README：新增核心功能、使用说明、项目结构、FAQ 等文档。

### 变更
- 优化自动保存：后端同步由「全量镜像」改为「增量同步」，防抖提升，高频编辑时显著减少 IPC 写入。
- 便签「输入口令查看（只读）」文案精简为「输入口令查看」。

### 作者
- 梦一闲

---

## [3.1.3] - 2026-09-05

### 修复
- 修复加密笔记在版本历史中字数统计错误：会话内解密时，「当前版本」改用解密后的明文统计字数与摘要，避免误显示为 0 字。
- 修复加密笔记内容往返丢失：后端同步加密笔记时保留密文（metadata 传输），重载后不再因覆盖丢失 `enc_data`，确保「输入口令 → 查看/编辑」不会丢失原文。
- 修复加密笔记解密后编辑区回填与二次重加密：改为「会话内解密查看可编辑 + 用同一口令重加密写回」的与便签一致的模型。

### 变更
- 笔记独立加密改为与便签一致的交互：设置口令加密 → 锁屏「输入口令查看」→ 本会话解密可编辑 → 保存用同一口令重加密写回密文，存储始终为密文。

### 新增
- 版本历史多版本持久化（SQLite 优先、Web 回退 localStorage）、里程碑快照、保留策略、差异字数与删除/清空能力。

### 作者
- 梦一闲

---

## [3.1.2] - 2026-09-05

### 变更
- 侧边栏 Logo 替换为品牌 logo（与 favicon / 应用图标统一）。

### 修复
- 修复标记私密后点击查看正文时内容丢失的问题：揭示正文时正确回填已保存内容，并在标记私密前即时落盘当前编辑内容。

### 作者
- 梦一闲

---

## [3.1.1] - 2026-09-04

### 变更
- 更换应用品牌 logo：以新 logo 重新生成 Tauri 全平台图标（Windows / macOS / iOS / Android）。
- 浏览器 favicon 同步更新为新的品牌 logo，并移除旧 favicon.svg。

### 作者
- 梦一闲

---

## [3.1.0] - 2026-09-03

### 新增
- 首版发布：个人知识管理桌面应用（React 19 + TypeScript + Tauri v2）。
- 完成历史遗留代码清理：删除零引用孤儿组件、调试残留与规划文档，构建产物瘦身。

### 修复
- 修复剪贴板「开启监听」无效（补齐 Tauri capability 事件权限并修正筛选逻辑）。

### 作者
- 梦一闲